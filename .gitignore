node_modules/
dist/
.DS_Store
*.local

# Headless playtest screenshots and log (scripts/playtest.mjs)
playtest-out/

# dev-server pid files from playtest runs
*.pid

# a node_modules symlink (agent worktrees) must never be committed either
node_modules
