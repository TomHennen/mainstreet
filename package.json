{
  "name": "mainstreet",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Engine for small, episodic, pixel-art town games.",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/TomHennen/mainstreet"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "build:site": "node scripts/build-site.mjs",
    "preview": "vite preview",
    "playtest": "node scripts/playtest.mjs",
    "test": "vitest run",
    "validate-episodes": "node scripts/validate-episodes.ts",
    "validate-assets": "node scripts/validate-assets.ts",
    "decode-art": "node scripts/decode-art.ts",
    "make-room": "node scripts/make-room.ts"
  },
  "dependencies": {
    "phaser": "^4.2.1"
  },
  "devDependencies": {
    "@types/node": "^22.19.0",
    "playwright": "^1.63.0",
    "typescript": "^5.9.3",
    "vite": "^8.2.2",
    "vitest": "^5.0.0"
  }
}
